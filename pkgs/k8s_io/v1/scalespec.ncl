# Module: k8s.io.v1.scalespec

{
  # ScaleSpec describes the attributes of a scale subresource.
  ScaleSpec = {
    replicas | optional | Number | doc "replicas is the desired number of instances for the scaled object.",
  },
}
